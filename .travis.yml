language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
install:
  - pip install coveralls
  - pip install -r requirements.txt
script:
  - coverage run --source=python manage.py test lists
after_success:
- coveralls
